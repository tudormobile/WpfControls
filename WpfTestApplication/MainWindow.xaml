<Window x:Class="WpfTestApplication.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfTestApplication" Background="AliceBlue"
        xmlns:controls="clr-namespace:Tudormobile.Wpf.Controls;assembly=Tudormobile.WpfControls"
        xmlns:behaviors="clr-namespace:Tudormobile.Wpf.Behaviors;assembly=Tudormobile.WpfControls"
        mc:Ignorable="d" AllowsTransparency="True" WindowStyle="None" ThemeMode="Light"
        Title="MainWindow" Height="450" Width="800">
    <DockPanel LastChildFill="True">
        <controls:TitleBar DockPanel.Dock="Top">
            <TextBlock Text="{Binding WindowTitle}" FontWeight="SemiBold"/>
        </controls:TitleBar>
        <Grid x:Name="root" Background="AliceBlue">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <CheckBox x:Name="checkBox" Content="Show Popup with TextBoxes inside" HorizontalAlignment="Center"/>
            <Popup Placement="Relative" AllowsTransparency="True" PopupAnimation="Slide" PlacementTarget="{Binding ElementName=root}"                   
                   IsOpen="{Binding ElementName=checkBox, Path=IsChecked}"
                   >
                <Border Padding="10">
                <Border SnapsToDevicePixels="True" Background="{Binding Background, RelativeSource={RelativeSource AncestorType=Window}}" 
                        BorderBrush="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Window}}"
                        BorderThickness="1" CornerRadius="10">
                <StackPanel Orientation="Vertical">
                    <TextBlock Text="Enter your name:" Margin="5"/>
                            <TextBox Width="200" Margin="5"
                             behaviors:TextBox.PlaceholderText="First Name" behaviors:TextBox.AutoSelect="True"
                            />
                </StackPanel>
                </Border>
                </Border>
            </Popup>
        </Grid>
        
    </DockPanel>
</Window>
