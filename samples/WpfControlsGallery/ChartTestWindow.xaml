<Window x:Class="WpfControlsGallery.ChartTestWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfControlsGallery"
        xmlns:controls="clr-namespace:Tudormobile.Wpf.Controls;assembly=Tudormobile.WpfControls"
        mc:Ignorable="d"
        Title="Chart Test Window" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="2*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <!-- PieChart, purple tint -->
        <controls:PieChart Series="{Binding PieChartData}"
                           Title="Simple Pie Chart"
                           Margin="0" Padding="20"
                           Grid.Row="0" Tint="Purple"
                           Grid.Column="1"/>
        <!-- BarChart, default tint -->
        <controls:BarChart Series="{Binding BarChartData}"
                           Title="Simple Bar Chart"
                           Margin="0" Padding="20"
                           Grid.Row="1"
                           Grid.Column="1"/>

        <!-- DonutChart, turquoise tint -->
        <controls:DonutChart Series="{Binding DonutChartData}"
                           Title="Simple Donut Chart" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                           Margin="0" Padding="0" Tint="Turquoise"
                           Grid.Row="2" Content="$1.23M"
                           Grid.Column="1"/>

        <ListBox Grid.Row="0" Grid.Column="0" ItemsSource="{Binding PieChartData.DataPoints}"/>
        <ListBox Grid.Row="1" Grid.Column="0" ItemsSource="{Binding BarChartData.DataPoints}"/>
        <ListBox Grid.Row="2" Grid.Column="0" ItemsSource="{Binding DonutChartData.DataPoints}"/>
    </Grid>
</Window>
