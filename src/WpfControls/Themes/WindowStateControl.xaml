<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:sys="clr-namespace:System;assembly=mscorlib"
                    xmlns:converters="clr-namespace:Tudormobile.Wpf.Converters"
                    xmlns:local="clr-namespace:Tudormobile.Wpf.Controls">

    <!-- Static resources that can potentially be override by consumer applications -->
    <Color x:Key="CloseBoxBackgroundHoverColor">#c42c1c</Color>
    <Color x:Key="CloseBoxBackgroundPressedColor">#d43c2c</Color>
    <sys:Double x:Key="MouseOverBackgroundPercentage">0.3</sys:Double>
    <sys:Double x:Key="MousePressBackgroundPercentage">0.2</sys:Double>
    <!-- Plain-style buttons for the WindowStateControl -->
    <Style TargetType="{x:Type Button}" x:Key="closeButtonStyle">
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Padding" Value="1"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Border x:Name="border" Background="{TemplateBinding Background}" 
                            BorderBrush="{TemplateBinding BorderBrush}" 
                            BorderThickness="{TemplateBinding BorderThickness}" 
                            SnapsToDevicePixels="true">
                        <ContentPresenter x:Name="contentPresenter" 
                                          Focusable="False" 
                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" 
                                          Margin="{TemplateBinding Padding}" 
                                          RecognizesAccessKey="True" 
                                          SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" 
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="true">
                            <Setter Property="Background" TargetName="border">
                                <Setter.Value>
                                    <SolidColorBrush Color="{StaticResource CloseBoxBackgroundHoverColor}"/>
                                </Setter.Value>
                            </Setter>
                            <Setter Property="Control.Foreground" TargetName="border" Value="White"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="true">
                            <Setter Property="Background" TargetName="border">
                                <Setter.Value>
                                    <SolidColorBrush Color="{StaticResource CloseBoxBackgroundPressedColor}"/>
                                </Setter.Value>
                            </Setter>
                            <Setter Property="Control.Foreground" TargetName="border" Value="White"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
    <Style TargetType="{x:Type Button}" x:Key="plainButtonStyle">
        <Style.Resources>
            <converters:ButtonBackgroundConverter x:Key="buttonBackgroundConverter"/>
        </Style.Resources>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Padding" Value="1"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Border x:Name="border" 
                            Background="{TemplateBinding Background}" 
                            BorderBrush="{TemplateBinding BorderBrush}" 
                            BorderThickness="{TemplateBinding BorderThickness}" 
                            SnapsToDevicePixels="true">
                        <ContentPresenter x:Name="contentPresenter" 
                                          Focusable="False" 
                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" 
                                          Margin="{TemplateBinding Padding}" 
                                          RecognizesAccessKey="True" 
                                          SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" 
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="true">
                            <Setter Property="Background" 
                                    TargetName="border" 
                                    Value="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=Foreground, Converter={StaticResource buttonBackgroundConverter}, ConverterParameter={StaticResource MouseOverBackgroundPercentage}}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="true">
                            <Setter Property="Background" 
                                    TargetName="border" 
                                    Value="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=Foreground, Converter={StaticResource buttonBackgroundConverter}, ConverterParameter={StaticResource MousePressBackgroundPercentage}}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="false">
                            <Setter Property="Opacity" Value=".25"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>


    <Style TargetType="{x:Type local:WindowStateControl}">
        <Setter Property="FontFamily" Value="Segoe Fluent Icons"/>
        <Setter Property="FontSize" Value="10"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Padding" Value="16,0"/>
        <Setter Property="MinHeight" Value="32"/>
        <Setter Property="HorizontalAlignment" Value="Right"/>
        <Setter Property="VerticalAlignment" Value="Top"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:WindowStateControl}">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                        <StackPanel Orientation="Horizontal" >
                            <Button Padding="{TemplateBinding Padding}" Style="{StaticResource plainButtonStyle}" Foreground="{TemplateBinding Foreground}" Background="{TemplateBinding Background}" x:Name="minButton" Content="&#xE921;" MinWidth="{Binding ActualHeight, RelativeSource={RelativeSource self}}" MinHeight="{TemplateBinding MinHeight}">
                                <Button.ToolTip>
                                    <ToolTip FontFamily="Segoe UI">
                                        <TextBlock Text="Minimize" />
                                    </ToolTip>
                                </Button.ToolTip>
                            </Button>
                            <Button Padding="{TemplateBinding Padding}" Style="{StaticResource plainButtonStyle}" Foreground="{TemplateBinding Foreground}" Background="{TemplateBinding Background}" x:Name="maxButton" Content="&#xE922;" MinWidth="{Binding ActualHeight, RelativeSource={RelativeSource self}}" MinHeight="{TemplateBinding MinHeight}" />
                            <Button Padding="{TemplateBinding Padding}" Style="{StaticResource closeButtonStyle}" Foreground="{TemplateBinding Foreground}" Background="{TemplateBinding Background}" x:Name="closeButton" Content="&#xe8bb;" MinWidth="{Binding ActualHeight, RelativeSource={RelativeSource self}}" MinHeight="{TemplateBinding MinHeight}">
                                <Button.ToolTip>
                                    <ToolTip FontFamily="Segoe UI">
                                        <TextBlock Text="Close" />
                                    </ToolTip>
                                </Button.ToolTip>
                            </Button>
                        </StackPanel>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>