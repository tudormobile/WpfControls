<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:local="clr-namespace:Tudormobile.Wpf.Controls"
                    xmlns:converters="clr-namespace:Tudormobile.Wpf.Converters"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Style TargetType="{x:Type local:SplitButton}">
        <Style.Resources>
            <converters:NullToVisibilityConverter x:Key="nullToVis"/>
            <converters:PlacementToGlyphConverter x:Key="placementToGlyph"/>
        </Style.Resources>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:SplitButton}">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                        <ToggleButton x:Name="PART_MainButton"
                                      HorizontalContentAlignment="Stretch"
                                      Command="{TemplateBinding Command}"
                                      CommandParameter="{TemplateBinding CommandParameter}"
                                      >
                            <ToggleButton.Content>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <ContentPresenter Content="{TemplateBinding Content}" x:Name="PART_ContentPresenter"
                                                      ContentTemplate="{TemplateBinding ContentTemplate}"
                                                      VerticalAlignment="Center"
                                                      HorizontalAlignment="Center"
                                                      Margin="{TemplateBinding Padding}"/>
                                    <Rectangle Grid.Column="1" Width="1" VerticalAlignment="Stretch" SnapsToDevicePixels="True"
                                               Margin="6,0" Visibility="{Binding Popup, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource nullToVis}}"
                                                Fill="LightGray"/>
                                               <!--Fill="{Binding ElementName=PART_MainButton, Path=Foreground}"/>-->
                                        <TextBlock Grid.Column="2" FontSize="8"
                                               FontFamily="Segoe MDL2 Assets"
                                               Visibility="{Binding Popup, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource nullToVis}}"
                                               Text="{Binding Placement, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource placementToGlyph}}"
                                               VerticalAlignment="Center" HorizontalAlignment="Right"
                                               Margin="0"/>
                                               <!--Text="&#xF08E;"-->
                                </Grid>
                            </ToggleButton.Content>
                        </ToggleButton>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>